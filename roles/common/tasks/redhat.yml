---
- name: Install dnf plugins core
  ansible.builtin.dnf:
    name: dnf-plugins-core
    state: present

- name: Enable EPEL (Oracle Linux package)
  ansible.builtin.dnf:
    name: "oracle-epel-release-el{{ ansible_distribution_major_version }}"
    state: present
  when:
    - guac_enable_epel | bool
    - ansible_distribution == "OracleLinux"
  register: _epel_ol
  failed_when: false

- name: Enable EPEL (generic epel-release rpm)
  ansible.builtin.dnf:
    name: "https://dl.fedoraproject.org/pub/epel/epel-release-latest-{{ ansible_distribution_major_version }}.noarch.rpm"
    state: present
    disable_gpg_check: true
  when:
    - guac_enable_epel | bool
    - ansible_distribution != "OracleLinux"
    - (_epel_ol is not defined) or (_epel_ol.rc | default(1) != 0)

- name: Enumerate available repository ids
  ansible.builtin.command: dnf -q repolist --all
  changed_when: false
  register: _repolist
  when: guac_enable_crb | bool

- name: Enable CRB-equivalent repository (dnf4 or dnf5 syntax)
  ansible.builtin.shell:
    cmd: >-
      dnf config-manager --set-enabled {{ item }} 2>/dev/null
      || dnf config-manager setopt {{ item }}.enabled=1 2>/dev/null
      || true
  changed_when: false
  loop:
    - "ol{{ ansible_distribution_major_version }}_codeready_builder"
    - "crb"
    - "powertools"
    - "codeready-builder-for-rhel-{{ ansible_distribution_major_version }}-{{ ansible_architecture }}-rpms"
  when:
    - guac_enable_crb | bool
    - _repolist.stdout is defined
    - item in _repolist.stdout

- name: Enable RPM Fusion free (optional, for ffmpeg-devel)
  ansible.builtin.dnf:
    name: "https://mirrors.rpmfusion.org/free/el/rpmfusion-free-release-{{ ansible_distribution_major_version }}.noarch.rpm"
    state: present
    disable_gpg_check: true
  when: guac_enable_rpmfusion | bool

- name: Refresh dnf metadata
  ansible.builtin.dnf:
    update_cache: true
  changed_when: false

- name: Install base packages
  ansible.builtin.dnf:
    name: "{{ common_base_packages }}"
    state: present

- name: Enable and start firewalld
  ansible.builtin.service:
    name: firewalld
    state: started
    enabled: true
  when:
    - guac_manage_firewall | bool
    - not (guac_container_build | bool)
  register: _fw_start
  failed_when: false

- name: Open firewall services (firewalld)
  ansible.posix.firewalld:
    service: "{{ item }}"
    permanent: true
    immediate: true
    state: enabled
  loop: [ssh, http, https]
  when:
    - guac_manage_firewall | bool
    - _fw_start.state | default('') == 'started'

- name: Toggle direct Guacamole port (firewalld)
  ansible.posix.firewalld:
    port: "8080/tcp"
    permanent: true
    immediate: true
    state: "{{ 'disabled' if guac_install_nginx | bool else 'enabled' }}"
  when:
    - guac_manage_firewall | bool
    - _fw_start.state | default('') == 'started'

- name: Open extra firewall ports (firewalld)
  ansible.posix.firewalld:
    port: "{{ item }}"
    permanent: true
    immediate: true
    state: enabled
  loop: "{{ guac_firewall_extra_ports }}"
  when:
    - guac_manage_firewall | bool
    - _fw_start.state | default('') == 'started'
