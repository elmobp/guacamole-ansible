---
- name: Restart mariadb
  ansible.builtin.service:
    name: mariadb
    state: restarted
  when: guac_install_mariadb | bool

# "Restart tomcat" is defined in the guacamole_client role and resolved play-wide.
